from setuptools import setup

requires =[
]

setup(name='grizzlies',
      version='0.0.1',
      description='',
      author='Ron Thompson',
      packages=['grizzlies'],
      install_requires=requires)
